- name: loops concepts
  hosts: local
  connection: local
  become: yes
  tasks:
    - name: print names
      ansible.builtin.debug:
        msg: "hello friends: {{ item }}"
      loop:
        - ram
        - rahim
        - sita 
        - gita